{
  "_comment": "Live Polymarket-priced order-book seeding bot. `autoLeagues` auto-seeds ALL v2 games of those leagues inside the active window [kickoff - preSeedLeadHours, kickoff + maxGameHours]; `markets[]` pins specific games or overrides a Polymarket slug. Each game reprices at pre.cadenceSec before kickoff and live.cadenceSec once started, then STOPS (flatten) when it resolves, Polymarket closes it, or it passes maxGameHours. maxExposureUsd is PER GAME notional — the wallet's USDC balance is the true global cap across all games. Edits apply live (config re-read each tick).",

  "defaults": {
    "live": {
      "_comment": "In-play: tighter exposure, wider spread, 2s cadence.",
      "rungs": 3,
      "topSpreadCents": 2,
      "stepCents": 1,
      "maxExposureUsd": 35,
      "cadenceSec": 2,
      "repriceDeadbandCents": 1,
      "inventoryCapShares": 100,
      "mergeMinShares": 5,
      "mergeCooldownSec": 30
    },
    "pre": {
      "_comment": "Pre-game (within preSeedNearHours of kickoff): tighter to fair, refresh every 60s.",
      "rungs": 3,
      "topSpreadCents": 1,
      "stepCents": 1,
      "maxExposureUsd": 35,
      "cadenceSec": 60,
      "repriceDeadbandCents": 1,
      "inventoryCapShares": 150,
      "mergeMinShares": 5,
      "mergeCooldownSec": 60
    },
    "far": {
      "_comment": "Early pre-game (preSeedNearHours..preSeedLeadHours out, e.g. 24-48h): same ladder as pre, just repriced hourly so far-out games are seeded without churn. Falls back to `pre` if omitted.",
      "rungs": 3,
      "topSpreadCents": 1,
      "stepCents": 1,
      "maxExposureUsd": 35,
      "cadenceSec": 3600,
      "repriceDeadbandCents": 1,
      "inventoryCapShares": 150,
      "mergeMinShares": 5,
      "mergeCooldownSec": 60
    }
  },

  "autoLeagues": ["MLB"],
  "preSeedLeadHours": 48,
  "preSeedNearHours": 24,
  "maxGameHours": 5,
  "maxMarkets": 24,

  "_rpc_comment": "RPC budget controls (the bot's ONLY on-chain reads are vault.markets + sharesOf; Polymarket prices + order placement are HTTP, not RPC). Cached reads are reused for the TTL; sharesOf is skipped entirely pre-game. maxRpcReadsPerSec is a HARD ceiling — over it, cached values are served. Monthly worst case ~= maxRpcReadsPerSec x 2.63M reads (~26 CU each on Alchemy). At 3/s that's a ceiling ~205M CU/mo; real steady-state is a small fraction of that.",
  "marketTtlSec": 300,
  "sharesTtlSec": 60,
  "maxRpcReadsPerSec": 3,

  "markets": [
    {
      "_comment": "Optional pinned games / slug overrides. Keeping the NFL HOF game so the bot cleanly flattens any leftover orders from the earlier test, then retires it.",
      "gameId": "NFL-CAR-ARI-2026-08-06-1786060800",
      "polymarketSlug": "nfl-car-ari-2026-08-07",
      "params": {}
    }
  ]
}
